name: push worflow
on: [push]
jobs:
    pushTest:
        runs-on: ubuntu-latest
        steps:
            - run: |
                echo $GITHUB_REF
                echo $GITHUB_SHA
            - uses: actions/checkout@v3
